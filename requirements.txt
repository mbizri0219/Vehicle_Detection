# Core YOLO dependencies
ultralytics>=8.0.0

# Computer Vision
opencv-python>=4.8.0
pillow>=10.0.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# System utilities
py-cpuinfo>=9.0.0

# PyTorch (downgraded to avoid compatibility issues with ultralytics)
# If you need GPU support, install from https://pytorch.org/get-started/locally/
torch==2.5.1
torchvision==0.20.1

# Jupyter/IPython (for notebook support)
ipython>=8.0.0
jupyter>=1.0.0

# Progress bars
tqdm>=4.65.0

# YAML support (if needed for config files)
pyyaml>=6.0

